import React from 'react';
import ConfirmMessage from '../../containers/ConfirmMessageContainer';

const ProfilePage = () => (
  <div className="profile">
    Profile
    <ConfirmMessage />
  </div>
);

export default ProfilePage;
